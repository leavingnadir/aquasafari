package com.aquasafari.backend.boat;

/**
 * Availability / assignment status shown as the "status indicators" in the Fleet Management
 * list (use-case main scenario, step 2).
 */
public enum BoatStatus {
    AVAILABLE,
    ASSIGNED,
    MAINTENANCE,
    INACTIVE
}
